// DEFINING FUNCTION TYPES
    // A NUMBER TO NUMBER FUNCTION
export type NumberToNumberFunction = (a: number) => number

// A TYPE FOR THE USERDATA
export type UserData = {
    id: number,
    username: string
}

// A TYPE FOR THE APPFORMDATA
export type AppFormData = { index: number }